language: node_js

node_js:
  - 8

install:
  - yarn install

script:
  - yarn build

before_deploy:
  - ./cleanup_build.sh

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  target_branch: master
  on:
    branch: source